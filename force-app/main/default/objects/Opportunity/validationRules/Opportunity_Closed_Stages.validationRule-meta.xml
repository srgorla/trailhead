<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Opportunity_Closed_Stages</fullName>
    <active>true</active>
    <description>Opportunity_Closed_Stages</description>
    <errorConditionFormula>AND(
    ISCHANGED(StageName),
    OR(
        ISPICKVAL(PRIORVALUE(StageName), &quot;Closed Won&quot;),
        ISPICKVAL(PRIORVALUE(StageName), &quot;Closed Lost&quot;)
    )
)</errorConditionFormula>
    <errorMessage>Opportunity Stage cant be changed once closed</errorMessage>
</ValidationRule>
